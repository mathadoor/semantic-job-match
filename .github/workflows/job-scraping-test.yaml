name: job-scraping-test

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  response-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: authenticate
        run: echo "Hello world"
        shell: bash